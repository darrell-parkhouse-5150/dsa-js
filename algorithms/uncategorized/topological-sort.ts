import { Graph } from "../../data-structures/graph/graph";
import { Stack } from "../../data-structures/stack/stack";

const topological_sort = (graph: Graph) => {
    let sorted = new Array();
    // let count = graph.nodes;
    let next: Stack = new Stack();
    
   
}